# User profile (Palimem dogfood)

## Preferences

- Prefers Python 3.13 for validation and service code
- Uses Cursor as the primary coding agent
- Timezone: US/Pacific

## Working style

- Runs `bash examples/claude-code/demo/phase6-readiness.sh` before pushing release changes
- Keeps commits focused; separates website repo from palimem repo
- Wants flat-repo paths (`app/`, `tests/`, `integrations/`) — not the legacy monorepo layout
